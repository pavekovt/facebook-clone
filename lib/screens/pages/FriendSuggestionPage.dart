import 'package:flutter/material.dart';

class FriendSuggestionPage extends StatelessWidget {
  const FriendSuggestionPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("FriendSuggestionPage"),
    );
  }
}